services:
  game:
    build: .
    restart: unless-stopped
    environment:
      HOST: 0.0.0.0
      PORT: 3111
    ports:
      - "${GAME_BIND:-0.0.0.0}:${GAME_PORT:-3111}:3111"
    read_only: true
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
